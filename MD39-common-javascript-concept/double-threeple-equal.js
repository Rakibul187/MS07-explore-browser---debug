const a = 1;
const b = true;
if (a == b) { console.log('a b are same values') }
else { console.log('a b are not same') } // a b are same values.
if (a === b) { console.log('a  b are same') }
else { console.log('a b are not same') } // a b are not same values
